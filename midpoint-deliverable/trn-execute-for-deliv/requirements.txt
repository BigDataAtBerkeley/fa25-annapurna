flask==3.0.0
psutil==5.9.0
requests==2.31.0
boto3>=1.28.0
opensearch-py>=2.0.0
requests-aws4auth>=1.3.0
python-dotenv>=1.0.0

# PyTorch and Neuron SDK (install separately with pip repos.neuron.amazonaws.com)
# torch-neuronx
# neuronx-cc
# torch==2.1.0
# neuron-profiler (install with: pip install neuron-profiler)

# HuggingFace libraries for NLP datasets
datasets>=2.14.0
transformers>=4.30.0

# Core ML libraries (should come with torch-neuronx, but listing for clarity)
# numpy (usually comes with torch)
# torch (comes with torch-neuronx)
# torch_xla (comes with torch-neuronx)
